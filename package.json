{
  "name": "request",
  "version": "1.0.0",
  "description": "a tool that combines use hooks to simplify swagger requests",
  "main": "api.js",
  "scripts": {
    "prettier_api": "prettier --write src/apis/**/*.ts src/types/**/*.ts",
    "api": "node api.js && npm run prettier_api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "try",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
